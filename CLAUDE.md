@AGENTS.md

## Context Navigation

When working on this project, always navigate via the knowledge graph first:

1. Read graphify-out/GRAPH_REPORT.md before answering any architecture or structure question
2. Use /graphify query "your question" to find specific connections, nodes, or relationships
3. Use /graphify path "NodeA" "NodeB" to trace how two concepts connect
4. Only read raw files if I explicitly say "read the file" — the graph is 59x more token-efficient
5. When in doubt about where something lives in the codebase, query the graph before grepping

The graph was built from the full kleinhans-digital codebase. Trust it.
